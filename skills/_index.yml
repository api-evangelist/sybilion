generated: '2026-08-11'
method: generated
source: openapi/sybilion-operational-api-openapi.yml + https://sybilion.dev/docs/
note: >-
  Searched first for provider-published skills — https://sybilion.dev/docs/AGENTS.md and
  /docs/llms.txt both return the VitePress docs shell (soft 404), and no AGENTS.md or skill file
  exists in the public Sybilion-AI GitHub org. These three are generated by API Evangelist from the
  harvested OpenAPI, the docs, and the artifacts in this repo. Steps cite method + path rather than
  operationId because the published spec declares none.
skills:
  - file: sybilion-forecast-and-explain.md
    name: Forecast a monthly series and explain what drove it
    api: openapi/sybilion-operational-api-openapi.yml
    operations:
      - 'GET /api/v1/me'
      - 'POST /api/v1/forecasts'
      - 'GET /api/v1/forecasts/{id}'
      - 'GET /api/v1/forecasts/{id}/artifacts/{name}'
    covers: [async job pattern, 402 vs 429 discrimination, 409 polling, artifact contract versioning, retention 404]
  - file: sybilion-rank-drivers-safely.md
    name: Rank external drivers for a series without double-billing
    api: openapi/sybilion-operational-api-openapi.yml
    operations:
      - 'GET /api/v1/regions'
      - 'GET /api/v1/categories'
      - 'GET /api/v1/me'
      - 'POST /api/v1/drivers'
      - 'GET /api/v1/usage'
    covers: [X-Request-ID billing idempotency, 502 retry rule, 503 is not transient, cost ceiling via filters.limit, usage reconciliation]
  - file: sybilion-connect-mcp-agent.md
    name: Use the Sybilion MCP connector from an agent
    api: mcp/sybilion-mcp.yml
    operations: [submit_forecast, get_forecast, get_forecast_chart, get_forecast_artifact, get_alerts, list_regions, list_categories]
    covers: [OAuth discovery chain, structured-argument client bug, billed write tools, the missing drivers tool]
